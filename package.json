{
  "name": "auto-reels",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:api": "pnpm --filter autoreels dev",
    "dev:web": "pnpm --filter auto-reels dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.26.2",
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "turbo": "^2.7.4"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
